# {{ project_name }}

One or two sentences about the problem, user, and value. Replace this after the project brief is approved.

<!-- project-dashboard:start -->
## Project Status

| Item | Current State |
|---|---|
| Type | {{ project_type }} |
| Runtime level | {{ runtime_level }} |
| Phase | Definition |
| Milestone | M-01 |
| Active task | T-001 |
| Next gate | ready-for-development |

> **Next step:** [Complete the active task](project/tasks/T-001-initialize-project.md).

Details: [Project Dashboard](project/index.md).
<!-- project-dashboard:end -->

## Quick Start

```bash
make setup
make dev
```

## Verify

```bash
make check
make build
```

{% if project_type == "frontend" %}
## Frontend Runtime

The Nuxt application lives in `frontend/` and is a standalone frontend. This
profile does not include a backend, external API integration, or generated
OpenAPI client; the generated TypeScript OpenAPI client belongs to a later
full-stack profile.

Install dependencies:

```bash
make setup
```

Run the Nuxt development server with HMR:

```bash
make dev
```

Build the production Nuxt output:

```bash
make build
```

`make build` writes the Nuxt server output to `frontend/.output/`. The server
entry point is `frontend/.output/server/index.mjs`.

Run the already built application without HMR:

```bash
make run
```

By default the built server listens on `http://127.0.0.1:8000`; override with
`HOST` and `PORT`. This is a shared-environment runtime smoke path, not a
production deployment.

Quality checks:

```bash
make check
```

`make check` validates the project dashboard, runs ESLint, Nuxt typecheck, and
Vitest. The main page composes a small `welcome` feature from `frontend/features/`.
{% endif %}

{% if project_type == "backend" %}
## Backend Runtime

Run the backend without auto-reload:

```bash
make run
```

Run the local development server with auto-reload:

```bash
make dev
```

The FastAPI entry point is `backend/src/app/main.py` (`app.main:app`). By default the
service listens on `http://127.0.0.1:8000`; override with `HOST` and `PORT`.

| Endpoint | Purpose |
|---|---|
| `GET /health` | Process health check. |
| `GET /ready` | Readiness check. Without external services, readiness means the app loaded successfully. |
| `GET /openapi.json` | OpenAPI document. |

`make build` writes a wheel and source distribution to `backend/dist/`. The wheel is
installable in a clean Python environment and can run with:

```bash
uvicorn app.main:app --host 127.0.0.1 --port 8000
```

This is a shared-environment baseline, not a production deployment.
{% endif %}

{% if project_type == "library" %}
## Package

Build artifacts are written to `dist/`. Verify the wheel from a clean environment:

```bash
uv venv /tmp/{{ project_slug }}-consumer
uv pip install --python /tmp/{{ project_slug }}-consumer/bin/python dist/*.whl
/tmp/{{ project_slug }}-consumer/bin/python -c "from {{ python_package }} import package_name; print(package_name())"
```

The public API is exported from `src/{{ python_package }}/__init__.py`. Publishing is not automated.
{% endif %}

## Where to Go

| Need | Open |
|---|---|
| Next step | `project/index.md` |
| Brief | `project/brief.md` |
| Roadmap | `project/roadmap.md` |
| Requirements | `project/requirements.md` |
| Architecture | `docs/architecture.md` |
| Workflow | `docs/workflow.md` |
| Quality | `docs/quality.md` |
| Decisions | `docs/decisions/index.md` |
| AI instructions | `AGENTS.md` |
