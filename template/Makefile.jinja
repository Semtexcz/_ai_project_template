.PHONY: setup dev run test format format-check lint typecheck api-schema api-generate api-check e2e e2e-production check build image-build image-inspect prod-up prod-status prod-smoke prod-down docs validate-docs validate-agent-skills{% if governance == "managed" %} project-status sync-project-docs validate-project task-ready task-start task-review task-complete task-block task-unblock task-cancel task-approve agent-status agent-context agent-pre-task agent-pre-review agent-post-task agent-review-report{% endif %} down

PROJECT_TYPE := {{ project_type }}
RUNTIME_LEVEL := {{ runtime_level }}
GOVERNANCE := {{ governance }}
WORKFLOW_MODE := {{ workflow_mode }}
PROJECT_SLUG := {{ project_slug }}
HOST ?= 127.0.0.1
PORT ?= 8000
PROD_BACKEND_PORT ?= 8000
PROD_FRONTEND_PORT ?= 3000
BACKEND_IMAGE ?= $(PROJECT_SLUG)-backend:production
FRONTEND_IMAGE ?= $(PROJECT_SLUG)-frontend:production
COMPOSE ?= docker compose
COREPACK_HOME ?= $(abspath .corepack)
PNPM ?= corepack pnpm
FRONTEND_PNPM = mkdir -p $(COREPACK_HOME) && cd frontend && COREPACK_HOME=$(COREPACK_HOME) $(PNPM)
PNPM_INSTALL_FLAGS ?=
OPENAPI_SCHEMA ?= artifacts/openapi.json

setup:
{% if project_type in ["script", "library"] %}
	uv sync
{% elif project_type == "backend" %}
	cd backend && uv sync
{% elif project_type == "frontend" %}
	$(FRONTEND_PNPM) install $(PNPM_INSTALL_FLAGS)
{% else %}
	cd backend && uv sync
	$(FRONTEND_PNPM) install $(PNPM_INSTALL_FLAGS)
	$(MAKE) api-schema
	$(MAKE) api-generate
{% endif %}

dev:
{% if project_type in ["script", "library"] %}
	uv run python -m {{ python_package }}
{% elif project_type == "backend" %}
	cd backend && uv run uvicorn app.main:app --app-dir src --host $(HOST) --port $(PORT) --reload
{% elif project_type == "frontend" %}
	$(FRONTEND_PNPM) dev
{% else %}
	python tools/fullstack.py dev
{% endif %}

run:
{% if project_type in ["script", "library"] %}
	uv run python -m {{ python_package }}
{% elif project_type == "backend" %}
	cd backend && uv run uvicorn app.main:app --app-dir src --host $(HOST) --port $(PORT)
{% elif project_type == "frontend" %}
	cd frontend && HOST=$(HOST) PORT=$(PORT) node .output/server/index.mjs
{% else %}
	python tools/fullstack.py run
{% endif %}

test:
{% if project_type in ["script", "library"] %}
	uv run pytest
{% elif project_type == "backend" %}
	cd backend && uv run pytest
{% elif project_type == "frontend" %}
	$(FRONTEND_PNPM) test
{% else %}
	cd backend && uv run pytest
	$(FRONTEND_PNPM) test
{% endif %}

lint:
{% if project_type in ["script", "library"] %}
	uv run ruff check .
{% elif project_type == "backend" %}
	cd backend && uv run ruff check .
{% elif project_type == "frontend" %}
	$(FRONTEND_PNPM) lint
{% else %}
	cd backend && uv run ruff check .
	$(FRONTEND_PNPM) lint
{% endif %}

format:
{% if project_type in ["script", "library"] %}
	uv run ruff format .
{% elif project_type == "backend" %}
	cd backend && uv run ruff format .
{% elif project_type == "frontend" %}
	@true
{% else %}
	cd backend && uv run ruff format .
{% endif %}

format-check:
{% if project_type in ["script", "library"] %}
	uv run ruff format --check .
{% elif project_type == "backend" %}
	cd backend && uv run ruff format --check .
{% elif project_type == "frontend" %}
	@true
{% else %}
	cd backend && uv run ruff format --check .
{% endif %}

typecheck:
{% if project_type in ["script", "library"] %}
	uv run pyright
{% elif project_type == "backend" %}
	cd backend && uv run pyright
{% elif project_type == "frontend" %}
	$(FRONTEND_PNPM) typecheck
{% else %}
	cd backend && uv run pyright
	$(FRONTEND_PNPM) typecheck
{% endif %}

api-schema:
{% if project_type == "fullstack" %}
	cd backend && uv run python -m app.export_openapi ../$(OPENAPI_SCHEMA)
{% else %}
	@true
{% endif %}

api-generate: api-schema
{% if project_type == "fullstack" %}
	python tools/api_client.py generate
{% else %}
	@true
{% endif %}

api-check: api-schema
{% if project_type == "fullstack" %}
	python tools/api_client.py check
{% else %}
	@true
{% endif %}

e2e:
{% if project_type == "fullstack" %}
	$(FRONTEND_PNPM) e2e
{% else %}
	@true
{% endif %}

e2e-production:
{% if project_type == "fullstack" and runtime_level == "production" %}
	mkdir -p $(COREPACK_HOME) && cd frontend && COREPACK_HOME=$(COREPACK_HOME) E2E_BASE_URL=$${E2E_BASE_URL:-http://127.0.0.1:$(PROD_FRONTEND_PORT)} $(PNPM) e2e
{% else %}
	@true
{% endif %}

{% if project_type == "fullstack" %}
check: validate-docs validate-agent-skills{% if governance == "managed" %} validate-project{% endif %} format-check lint typecheck api-check test
{% else %}
check: validate-docs validate-agent-skills{% if governance == "managed" %} validate-project{% endif %} format-check lint typecheck test
{% endif %}

build:
{% if project_type in ["script", "library"] %}
	rm -rf dist
	uv build
{% elif project_type == "backend" %}
	cd backend && uv build
{% elif project_type == "frontend" %}
	rm -rf frontend/.output
	$(FRONTEND_PNPM) build
{% else %}
	$(MAKE) api-check
	rm -rf backend/dist frontend/.output
	cd backend && uv build
	$(FRONTEND_PNPM) build
{% endif %}

image-build:
{% if project_type == "fullstack" and runtime_level == "production" %}
	$(COMPOSE) build
{% elif project_type == "backend" and runtime_level == "production" %}
	docker build -t $(BACKEND_IMAGE) backend
{% elif project_type == "frontend" and runtime_level == "production" %}
	docker build -t $(FRONTEND_IMAGE) frontend
{% else %}
	@true
{% endif %}

image-inspect:
{% if project_type == "fullstack" and runtime_level == "production" %}
	BACKEND_IMAGE=$(BACKEND_IMAGE) FRONTEND_IMAGE=$(FRONTEND_IMAGE) python tools/fullstack.py image-inspect
{% else %}
	@true
{% endif %}

prod-up:
{% if project_type == "fullstack" and runtime_level == "production" %}
	BACKEND_IMAGE=$(BACKEND_IMAGE) FRONTEND_IMAGE=$(FRONTEND_IMAGE) PROD_BACKEND_PORT=$(PROD_BACKEND_PORT) PROD_FRONTEND_PORT=$(PROD_FRONTEND_PORT) $(COMPOSE) up -d
{% else %}
	@true
{% endif %}

prod-status:
{% if project_type == "fullstack" and runtime_level == "production" %}
	BACKEND_IMAGE=$(BACKEND_IMAGE) FRONTEND_IMAGE=$(FRONTEND_IMAGE) PROD_BACKEND_PORT=$(PROD_BACKEND_PORT) PROD_FRONTEND_PORT=$(PROD_FRONTEND_PORT) COMPOSE='$(COMPOSE)' python tools/fullstack.py prod-status
{% else %}
	@true
{% endif %}

prod-smoke:
{% if project_type == "fullstack" and runtime_level == "production" %}
	PROD_BACKEND_URL=$${PROD_BACKEND_URL:-http://127.0.0.1:$(PROD_BACKEND_PORT)} PROD_FRONTEND_URL=$${PROD_FRONTEND_URL:-http://127.0.0.1:$(PROD_FRONTEND_PORT)} python tools/fullstack.py prod-smoke
{% else %}
	@true
{% endif %}

prod-down:
{% if project_type == "fullstack" and runtime_level == "production" %}
	BACKEND_IMAGE=$(BACKEND_IMAGE) FRONTEND_IMAGE=$(FRONTEND_IMAGE) PROD_BACKEND_PORT=$(PROD_BACKEND_PORT) PROD_FRONTEND_PORT=$(PROD_FRONTEND_PORT) $(COMPOSE) down --timeout 10
{% else %}
	@true
{% endif %}

docs:{% if governance == "managed" %} project-status{% endif %}
{% if governance == "managed" %}
	python tools/project.py status
{% else %}
	@echo "Project docs live under docs/ and project/brief.md."
{% endif %}

{% if governance == "managed" %}
project-status:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py status

sync-project-docs:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py sync

validate-project:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py validate

{% endif %}

validate-docs:
{% if governance == "managed" %}
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py validate-docs
{% else %}
	@test -f README.md
	@test -f AGENTS.md
	@test -f docs/architecture.md
	@test -f docs/product.md
	@test -f docs/workflow.md
	@test -f docs/quality.md
	@test -f project/brief.md
{% endif %}

validate-agent-skills:
	PYTHONDONTWRITEBYTECODE=1 python tools/agent.py validate-skills

{% if governance == "managed" %}
agent-status:
	PYTHONDONTWRITEBYTECODE=1 python tools/agent.py status

agent-context:
	PYTHONDONTWRITEBYTECODE=1 python tools/agent.py context --task $(TASK) $(if $(SKILL),--skill $(SKILL),) $(if $(MODE),--mode $(MODE),) $(if $(FORMAT),--format $(FORMAT),)

agent-pre-task:
	PYTHONDONTWRITEBYTECODE=1 python tools/agent.py pre-task --task $(TASK)

agent-pre-review:
	PYTHONDONTWRITEBYTECODE=1 python tools/agent.py pre-review --task $(TASK)

agent-post-task:
	PYTHONDONTWRITEBYTECODE=1 python tools/agent.py post-task --task $(TASK)

agent-review-report:
	PYTHONDONTWRITEBYTECODE=1 python tools/agent.py context --task $(TASK)

task-ready:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py ready $(TASK)

task-start:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py start $(TASK)

task-review:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py review $(TASK)

task-complete:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py complete $(TASK)

task-block:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py block $(TASK) --reason "$(REASON)" --unblock "$(UNBLOCK)"

task-unblock:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py unblock $(TASK)

task-cancel:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py cancel $(TASK)

task-approve:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py approve $(TASK) --approved-by "$(APPROVED_BY)"

{% if workflow_mode == "pr" %}
.PHONY: pr-validate

# Pre-merge structural validation for governed pull requests. Consumes the PR
# context from environment variables (PR_HEAD_REF, PR_TITLE, PR_BODY) and
# fails unless the pull request deterministically references one review-ready
# task. Run this in CI before a human merges a managed pull request.
pr-validate:
	PYTHONDONTWRITEBYTECODE=1 python tools/project.py pr-validate
{% endif %}
{% endif %}

down:
{% if project_type == "fullstack" and runtime_level == "production" %}
	$(MAKE) prod-down
{% elif project_type == "fullstack" %}
	@true
{% else %}
	@true
{% endif %}
