schema_version: 2
required:
  - schema_version
  - bootstrap
  - task
  - exclude

bootstrap:
  required:
    - files

budget:
  properties:
    max_files:
      type: integer
      minimum: 1
    max_bytes:
      type: integer
      minimum: 1
